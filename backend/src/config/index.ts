import {
  CORS_ALLOWED_ORIGINS,
  DATABASE_URL,
  GOOGLE_CLIENT_ID,
  GOOGLE_CLIENT_SECRET,
  GOOGLE_REDIRECT_URI,
  JWT_ACCESS_TTL,
  JWT_REFRESH_TTL_DAYS,
  JWT_SECRET,
  NODE_ENV,
  OAUTH_DEFAULT_EXTENSION_REDIRECT_URI,
  GEMINI_API_KEY,
  PORT,
  REQUEST_BODY_LIMIT,
  AHREFS_URL,
  REDIS_URL,
  SEMRUSH_URL,
  SEMRUSH_LOG_FULL_RESPONSE,
} from './env';

const config = {
  PORT,
  NODE_ENV,
  DATABASE_URL,
  JWT_SECRET,
  JWT_ACCESS_TTL,
  JWT_REFRESH_TTL_DAYS,
  GOOGLE_CLIENT_ID,
  GOOGLE_CLIENT_SECRET,
  GOOGLE_REDIRECT_URI,
  OAUTH_DEFAULT_EXTENSION_REDIRECT_URI,
  CORS_ALLOWED_ORIGINS,
  REQUEST_BODY_LIMIT,
  AHREFS_URL,
  REDIS_URL,
  GEMINI_API_KEY,
  SEMRUSH_URL,
  SEMRUSH_LOG_FULL_RESPONSE,
} as const;

export default config;
